window.onload = function(){
    var canvas = document.querySelector('canvas');
    // 获取渲染上下文对象（画笔）
    var ctx = canvas.getContext('2d');

    // 绘制图像
    ctx.fillRect(100,50,200,100);   // x , y , width , height

    // 
}